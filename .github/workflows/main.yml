name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Install dnsutils
      run: | 
          apt-get install dnsutils
          nslookup natwork.com

